# Anchored to repo root — unanchored `data/` also matches src/lpr/data/ (the
# dataset package), which silently excluded it from the first three pushes.
/data/
/weights/
/artifacts/
/runs/
/experiments/
__pycache__/
*.pyc
